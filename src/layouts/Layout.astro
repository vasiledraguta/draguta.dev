---
import "../styles/global.css";
import { Font } from "astro:assets";
import { ClientRouter } from "astro:transitions";
import VideoBackground from "@/components/VideoBackground.astro";
import Links from "@/components/Links.astro";
import Footer from "@/components/Footer.astro";

interface Props {
  title?: string;
  description?: string;
  image?: string;
  imageAlt?: string;
  noindex?: boolean;
}

const {
  title = "draguta.dev",
  description = "a little bit of code, a little bit of design, a little bit of delusion",
  image = "/og-image.png",
  imageAlt = "A meadow and hills, from the site's background video.",
  noindex = false,
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const absoluteImage = new URL(image, Astro.site);
const fullTitle = title === "draguta.dev" ? title : `${title} | draguta.dev`;

const jsonLd = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "Person",
      name: "Vasile Draguța",
      url: "https://draguta.dev",
    },
    {
      "@type": "WebSite",
      name: "draguta.dev",
      url: "https://draguta.dev",
    },
  ],
};
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- SEO -->
    <title>{fullTitle}</title>
    <meta name="description" content={description} />
    <meta name="author" content="Vasile Draguța" />
    <link rel="canonical" href={canonicalURL} />
    <script
      is:inline
      type="application/ld+json"
      set:html={JSON.stringify(jsonLd)}
    />

    <!-- Theme Color -->
    <meta
      name="theme-color"
      content="#f2f0e8"
      media="(prefers-color-scheme: light)"
    />
    <meta
      name="theme-color"
      content="#0a0a0a"
      media="(prefers-color-scheme: dark)"
    />

    <!-- Robots -->
    {
      noindex ? (
        <meta name="robots" content="noindex, nofollow" />
      ) : (
        <Fragment>
          <meta name="robots" content="index, follow" />
          <meta
            name="googlebot"
            content="index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1"
          />
        </Fragment>
      )
    }

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:site_name" content="draguta.dev" />
    <meta property="og:locale" content="en_US" />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={absoluteImage} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content={imageAlt} />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content="@vasiledraguta" />
    <meta name="twitter:creator" content="@vasiledraguta" />
    <meta name="twitter:title" content={fullTitle} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={absoluteImage} />
    <meta name="twitter:image:alt" content={imageAlt} />

    <!-- Favicon -->
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="alternate icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />

    <!-- Fonts -->
    <Font cssVariable="--font-onest" preload />
    <Font cssVariable="--font-jetbrains-mono" />

    <ClientRouter />
    <script is:inline>
      (function () {
        function preference() {
          var stored = null;

          try {
            stored = localStorage.getItem("theme");
          } catch (e) {
            stored = null;
          }

          if (stored === "light" || stored === "dark") return stored;

          var system = window.matchMedia("(prefers-color-scheme: dark)").matches
            ? "dark"
            : "light";

          try {
            localStorage.setItem("theme", system);
          } catch (e) {
            return system;
          }

          return system;
        }

        function applyTheme() {
          document.documentElement.classList.toggle(
            "dark",
            preference() === "dark"
          );
        }

        applyTheme();

        if (!window.__themeInit) {
          window.__themeInit = true;
          document.addEventListener("astro:after-swap", applyTheme);
        }
      })();
    </script>
  </head>
  <body>
    <VideoBackground />
    <p id="zen-hint">
      <button
        id="zen-exit"
        type="button"
        class="inline-flex items-baseline gap-2 cursor-pointer tap"
      >
        <kbd class="font-mono text-xs">z</kbd> to exit
      </button>
    </p>
    <div id="site-content" class="transition-opacity duration-500">
      <div
        class="relative z-10 flex flex-col min-h-screen mx-auto px-8 max-w-4xl"
      >
        <header class="py-6">
          <Links />
        </header>
        <main class="flex-1 flex flex-col">
          <slot />
        </main>
        <Footer />
      </div>
    </div>
    <script>
      import { initVideo } from "@/lib/video";
      import { initShortcuts } from "@/lib/shortcuts";

      try {
        initShortcuts();
      } catch (error) {
        console.error("Failed to init keyboard shortcuts:", error);
      }

      try {
        initVideo();
      } catch (error) {
        console.error("Failed to init background video:", error);
      }
    </script>
  </body>
</html>
